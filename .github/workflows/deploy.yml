name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - beta

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      # --- MIEJSCE NA TWÓJ BUILD (OPCJONALNE) ---
      # Jeśli używasz np. npm, odkomentuj i dostosuj poniższe:
      # - name: Install and Build 🔧
      #   run: |
      #     npm install
      #     npm run build
      # ------------------------------------------

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages # Branch, na którym ląduje gotowa strona
          folder: .        # Folder źródłowy (np. 'dist' lub 'build' jeśli budujesz stronę)
          
          # Logika warunkowa:
          # Jeśli main -> wrzuć do głównego katalogu (root)
          # Jeśli beta -> wrzuć do folderu 'beta'
          target-folder: ${{ github.ref == 'refs/heads/main' && '.' || 'beta' }}
          
          # KLUCZOWE: Zapobiega usunięciu folderu 'beta' gdy deployuje 'main'
          clean-exclude: |
            beta